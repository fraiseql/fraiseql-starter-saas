# PostgreSQL connection string — matches docker-compose.yml defaults
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/saas

# NATS connection string — omit or leave blank to disable event publishing
# Requires the nats service in docker-compose.yml
NATS_URL=nats://localhost:4222
